{
  "name": "nalgorithm-web",
  "version": "0.8.2",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node ../scripts/sync-humanizer.mjs",
    "dev": "vite",
    "prebuild": "node ../scripts/sync-humanizer.mjs",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "pretypecheck": "node ../scripts/sync-humanizer.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "nalgorithm": "*",
    "nostr-tools": "^2.10.4",
    "qrcode-generator": "^2.0.4"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
